
import OnboardingContainer from "@/components/OnboardingContainer";

const Index = () => {
  // Simplified version that doesn't depend on Clerk authentication
  return <OnboardingContainer />;
};

export default Index;
